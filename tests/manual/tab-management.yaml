category: tab-management
display_name: "Tab Management"

tests:
  # --- Drag reorder (stays manual — WebDriver drag-and-drop is unreliable) ---

  - id: MT-TAB-05
    name: "Drag tab to new position"
    platforms: [all]
    prerequisites:
      - app: true
    instructions:
      - "Open multiple terminal tabs"
      - "Drag a tab to a new position in the tab bar"
    expected:
      - "Tab moves to new position, order persists"
    verification: manual

  - id: MT-TAB-06
    name: "Drag tab to edge creates new split"
    platforms: [all]
    prerequisites:
      - app: true
    instructions:
      - "Drag a tab to the edge of another panel"
    expected:
      - "New split created, tab moves to new panel"
    verification: manual

  - id: MT-TAB-07
    name: "Drag tab between groups"
    platforms: [all]
    prerequisites:
      - app: true
    instructions:
      - "Open multiple tabs in a split view"
      - "Drag a tab from one group to another"
    expected:
      - "Tab moves to the target group"
    verification: manual

  # --- Save terminal content to file (PR #35) ---

  - id: MT-TAB-08
    name: "Save to File — native dialog opens"
    pr: 35
    platforms: [all]
    prerequisites:
      - app: true
    instructions:
      - "Open a terminal with some output"
      - "Right-click the terminal tab or use the menu"
      - "Click 'Save to File'"
    expected:
      - "Native save dialog opens with default filename 'terminal-output.txt'"
    verification: manual
    tags: [native-dialog]

  - id: MT-TAB-09
    name: "Save to File — file is written"
    pr: 35
    platforms: [all]
    prerequisites:
      - app: true
    instructions:
      - "In the save dialog, choose a location and save"
      - "Open the saved file"
    expected:
      - "File is written with the terminal's text content"
    verification: manual

  - id: MT-TAB-10
    name: "Save to File — cancel does nothing"
    pr: 35
    platforms: [all]
    prerequisites:
      - app: true
    instructions:
      - "Click 'Save to File' then cancel the dialog"
    expected:
      - "Nothing happens (no crash, no file written)"
    verification: manual

  # --- Split Views ---

  - id: MT-TAB-16
    name: "Drag divider resizes split panels"
    platforms: [all]
    prerequisites:
      - app: true
    instructions:
      - "Open a split view"
      - "Drag the divider between split panels"
    expected:
      - "Panels resize, terminals re-fit"
    verification: manual
