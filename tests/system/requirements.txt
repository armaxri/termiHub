# termiHub Python system-test harness dependencies.
pytest>=8.0
websockets>=12.0
psutil>=5.9
# tftpy: real TFTP RRQ client for the Embedded Services SVC-13 transfer check
# (replaces host-curl, whose tftp:// support is build-dependent — issue #964).
tftpy>=0.8
# pyftpdlib: in-process FTP server for the app-independent transfer-checker tests
# (test-only; the FTP checker itself uses only the stdlib ftplib).
pyftpdlib>=1.5
