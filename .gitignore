# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
coverage
test-results
tests/reports/*.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea
.DS_Store
.claude/*
!.claude/CLAUDE.md
!.claude/settings.json
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Cargo workspace target directory
/target/

# Windows reserved device names (created accidentally by Git Bash when scripts
# use redirection to NUL, CON, etc. instead of /dev/null).
NUL
CON
PRN
AUX

# Lock files from other package managers (we use pnpm)
package-lock.json
yarn.lock
