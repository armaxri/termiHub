### Fixed

- System monitor now shows an explicit "Reconnecting…" badge and dims the CPU/memory/disk
  readings while a monitoring connection is being re-established, so the frozen last-known
  numbers are no longer mistaken for live data (SM-014).
