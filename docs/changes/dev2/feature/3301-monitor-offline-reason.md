### Changed

- An offline system monitor now says why it went offline — "remote output unreadable",
  "connection lost", or "no data from agent" — in the status bar and the Open Connections
  list, instead of always "connection lost" (#3301).
