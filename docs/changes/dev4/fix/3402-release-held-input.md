### Fixed

- Remote desktop (VNC/RDP): a key or mouse button held down no longer stays stuck
  on the remote. termiHub now releases everything still pressed when another
  window takes the remote desktop over, when the connection automatically
  reconnects, and when the remote desktop view, its window, or the app loses
  focus or is hidden.
