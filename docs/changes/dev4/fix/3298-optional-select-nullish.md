### Fixed

- Connection editor: editing a remote agent (or connection) whose saved
  settings omit an optional dropdown field — e.g. an agent created before the
  update-strategy setting existed — no longer leaves Save silently disabled.
