### Fixed

- The command palette now lists the tab-group navigation commands — **Close Tab
  Group**, **Next Tab Group**, and **Previous Tab Group**. They had keyboard
  shortcuts but were silently missing from the palette, so they could only be
  triggered by their shortcut. They appear whenever more than one tab group is
  open and run the same action as the shortcut (UX-028).
