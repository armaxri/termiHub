### Changed

- The Macro, Workflow, and Recent Sessions search boxes now show a clear button
  (an "x") while a query is typed, so a filter can be reset in one click instead
  of selecting and deleting the text. The Recent Sessions box also gains the same
  leading search icon the other two already had, so all three read the same
  (UISF-004).
