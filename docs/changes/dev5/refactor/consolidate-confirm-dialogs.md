### Changed

- The "Large Paste" confirmation now focuses its Cancel button when it opens and
  confirms on Enter (unless Cancel holds focus), matching every other
  confirmation dialog in the app. This comes from moving it onto the shared
  confirm-dialog primitive (UISF-006).
