### Fixed

- Connection editor: the password / key-passphrase prompt opened by **Test** no longer shows a
  "Save password" checkbox. Test never stores a secret, so the checkbox did nothing; the prompt
  opened by **Save & Connect** still offers it (#3316).
