### Fixed

- The Transfer Queue and the file browser's transfer footer now show Pause,
  Resume and Retry for SSH/SFTP transfers. SFTP transfers have supported these
  controls since they moved onto the transfer queue, but the buttons were still
  hidden for every connection type except FTP (#3304).
