### Added

- Schedules keep a history of their recent attempts: expand a schedule in the
  **Schedules** list to see its last 20 attempts — when each ran, how long it
  took, whether it completed, failed or was skipped and why (not connected,
  previous run still in progress, missed while termiHub was closed), and how many
  workflow runs it recorded in the run history (#3528).
