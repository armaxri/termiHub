### Changed

- Elevation shadows are now tuned per theme (UI-012): light themes get softer,
  lower-alpha shadows instead of the dark theme's heavy dark shadows, so panels,
  dropdowns, and dialogs read with correct depth on the light theme's pale
  surfaces. The dark theme is unchanged.
