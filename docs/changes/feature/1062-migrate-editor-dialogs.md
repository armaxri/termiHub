## Changed

- The icon picker, unsaved-changes prompt, save-workspace dialog, and embedded-server configuration dialog now use the shared modal and button components — consistent styling, motion, and focus. The save-workspace dialog's button text also renders correctly in the light theme (was hardcoded white).
