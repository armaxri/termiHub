## Changed

- The password prompt, credential-store unlock, master-password setup, and connection import/export dialogs now use the shared modal and button components — consistent styling, motion, and focus handling. Unlock, import, and export actions show a pending (spinner) state while they run.
