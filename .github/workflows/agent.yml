name: Agent

on:
  pull_request:
    branches: [ main, develop ]
    paths:
      - 'agent/**'
      - 'core/**'
      - 'Cargo.toml'

# Cancel an in-flight agent build when a newer commit lands on the same PR.
concurrency:
  group: agent-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-linux:
    name: Build (${{ matrix.target }})
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-unknown-linux-musl
            artifact_name: termihub-agent-linux-x64
          - target: aarch64-unknown-linux-musl
            artifact_name: termihub-agent-linux-arm64
          - target: armv7-unknown-linux-musleabihf
            artifact_name: termihub-agent-linux-armv7

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.target }}

      - name: Install cross-rs
        run: |
          curl -fsSL https://github.com/cross-rs/cross/releases/latest/download/cross-x86_64-unknown-linux-gnu.tar.gz \
            | sudo tar xz -C /usr/local/bin

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build custom cross-rs image
        uses: docker/build-push-action@v7
        with:
          context: agent/docker
          file: agent/docker/Dockerfile.${{ matrix.target }}
          tags: localhost/termihub-cross:${{ matrix.target }}
          load: true
          cache-from: type=gha,scope=${{ matrix.target }}
          cache-to: type=gha,mode=max,scope=${{ matrix.target }}

      - name: Build
        env:
          TERMIHUB_BUILD_BRANCH: ${{ github.head_ref || github.ref_name }}
        run: CROSS_CONFIG=agent/Cross.toml cross build --release --target ${{ matrix.target }} -p termihub-agent

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.artifact_name }}
          path: target/${{ matrix.target }}/release/termihub-agent
          if-no-files-found: error
          retention-days: 7

  # Build the Windows agent (MSVC) and run the agent/core test suites on a
  # Windows runner. cross-rs cannot build the MSVC ABI, so this uses native
  # cargo on windows-latest. System/E2E tests stay Linux-only (tauri-driver +
  # Docker — see ADR-5); Windows agent coverage is unit/integration + manual.
  build-windows:
    name: Build (${{ matrix.target }})
    runs-on: windows-latest

    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-pc-windows-msvc
            artifact_name: termihub-agent-windows-x64

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.target }}

      - name: Run agent/core tests
        run: cargo test -p termihub-agent -p termihub-core --all-features

      - name: Build
        env:
          TERMIHUB_BUILD_BRANCH: ${{ github.head_ref || github.ref_name }}
        run: cargo build --release --target ${{ matrix.target }} -p termihub-agent

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.artifact_name }}
          path: target/${{ matrix.target }}/release/termihub-agent.exe
          if-no-files-found: error
          retention-days: 7

  build-macos:
    name: Build (${{ matrix.target }})
    runs-on: macos-latest

    strategy:
      fail-fast: false
      matrix:
        include:
          - target: aarch64-apple-darwin
            artifact_name: termihub-agent-macos-arm64
          - target: x86_64-apple-darwin
            artifact_name: termihub-agent-macos-x64

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.target }}

      - name: Build
        env:
          TERMIHUB_BUILD_BRANCH: ${{ github.head_ref || github.ref_name }}
        run: cargo build --release --target ${{ matrix.target }} -p termihub-agent

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.artifact_name }}
          path: target/${{ matrix.target }}/release/termihub-agent
          if-no-files-found: error
          retention-days: 7
