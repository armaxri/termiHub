/// Run the NDJSON stdio transport loop.
///
/// Reads JSON-RPC messages from stdin (one per line) and writes
/// responses to stdout.
pub async fn run_stdio_loop() -> anyhow::Result<()> {
    // Stub — will be implemented in a later commit
    Ok(())
}
