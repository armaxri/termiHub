{
  "_comment": "Example dev.local.json for parallel checkout 'dev3'. Copy it into the repo root as dev.local.json (`cp examples/dev3.dev.local.json dev.local.json`). Each checkout must use a DISTINCT example (dev0..dev3) so their dev servers, dev agents and Docker test ports never collide. See docs/testing.md → 'Parallel test isolation'.",
  "dev_port": 1450,
  "dev_agent_port": 2252,
  "dev_name": "dev3",
  "compose_project": "termihub-test-3",
  "test_port_offset": 3000
}
